# 第五章 传输层

## 章节定位

传输层位于网络层之上、应用层之下，核心任务是为**应用进程之间**提供端到端通信服务。

- 网络层：主机到主机的逻辑通信
- 传输层：进程到进程的逻辑通信
- 传输层主要协议：
  - UDP：无连接、不可靠、面向报文
  - TCP：面向连接、可靠、面向字节流

---

## 传输层提供的服务

### 基本功能

1. **进程到进程通信**
   - 通过端口号标识应用进程。
   - IP 地址标识主机，端口号标识主机内的进程。

2. **复用与分用**
   - 复用：发送方多个应用进程共用一个传输层协议向下发送。
   - 分用：接收方传输层根据端口号把数据交给正确的应用进程。

3. **差错检测**
   - UDP、TCP 都对首部和数据进行检验。
   - IP 只检验 IP 首部，不检验数据部分。

4. **提供两类服务**
   - 无连接服务：UDP
   - 面向连接服务：TCP

---

## 端口号与套接字

### 端口号

端口号长度为 `16 bit`，范围：`0 ~ 65535`。

| 端口范围 | 名称 | 用途 |
|---|---|---|
| 0 ~ 1023 | 熟知端口号 | 分配给常见服务 |
| 1024 ~ 49151 | 登记端口号 | 给用户进程或应用登记使用 |
| 49152 ~ 65535 | 短暂端口号 | 客户端临时使用 |

常见熟知端口：

| 应用 | 端口 |
|---|---|
| FTP 控制连接 | 21 |
| TELNET | 23 |
| SMTP | 25 |
| DNS | 53 |
| HTTP | 80 |
| POP3 | 110 |
| HTTPS | 443 |

注意：

- TCP 和 UDP 的端口号空间相互独立。
- 同一个端口号在不同主机上没有直接关系。
- 客户端通常使用短暂端口号。
- 服务器通常使用熟知端口号或登记端口号。

### 套接字 Socket

套接字：`Socket = IP 地址 : 端口号`

一条 TCP 连接由两个端点唯一确定：

`TCP 连接 = {源 IP, 源端口, 目的 IP, 目的端口}`

注意：

- 一个 IP 地址可以参与多个 TCP 连接。
- 一个端口号也可以出现在多个 TCP 连接中。
- 只看端口号不能唯一确定 TCP 连接。

---

# UDP

## UDP 的特点

UDP 只在 IP 的基础上增加了：

1. 复用
2. 分用
3. 差错检测

UDP 的特点：

- 无连接，不需要建立连接。
- 不可靠，尽最大努力交付。
- 面向报文，保留应用层报文边界。
- 首部开销小，固定 `8B`。
- 支持一对一、一对多、多对多通信。
- 无拥塞控制，发送速率不受网络拥塞直接限制。

适用场景：

- DNS
- DHCP
- 视频会议
- 流媒体
- 实时语音
- 少量请求-响应型应用

### UDP 面向报文

应用层交给 UDP 一个报文，UDP 添加首部后直接交给 IP。

UDP 不拆分、不合并应用层报文。

所以：

- 报文太大：交给 IP 后可能被分片。
- 报文太小：首部开销比例较大。

---

## UDP 数据报格式

UDP 数据报 = UDP 首部 + 数据。

UDP 首部固定 `8B`，包含 4 个字段，每个字段 `2B`：

| 字段 | 长度 | 含义 |
|---|---:|---|
| 源端口 | 2B | 发送进程端口；不需要回复时可为 0 |
| 目的端口 | 2B | 接收进程端口，必须有效 |
| 长度 | 2B | UDP 首部 + UDP 数据的总长度 |
| 检验和 | 2B | 检验首部和数据 |

UDP 长度字段最小值：`8B`。

若接收方没有对应端口：

- 丢弃该 UDP 数据报；
- 可能发送 ICMP 端口不可达报文。

---

## UDP 检验和

UDP 检验和检验：

`伪首部 + UDP 首部 + UDP 数据`

### 伪首部

伪首部不是真正的 UDP 首部，只参与检验和计算，不向下传递，也不向上交付。

IPv4 中 UDP 伪首部长度为 `12B`，包括：

- 源 IP 地址
- 目的 IP 地址
- 全 0 字段
- 协议号
- UDP 长度

UDP 协议号：`17`。

### 检验和计算规则

发送方：

1. 检验和字段置 0。
2. 拼接伪首部、UDP 首部、UDP 数据。
3. 若总长度为奇数字节，末尾补 0。
4. 按 16 bit 分组进行二进制反码求和。
5. 将结果取反，填入检验和字段。

接收方：

1. 重新拼接伪首部、UDP 首部、UDP 数据。
2. 按 16 bit 反码求和。
3. 结果若为全 1，说明无差错。
4. 否则丢弃。

注意：

- IP 首部检验和只检验 IP 首部。
- UDP 检验和检验 UDP 首部和数据。
- TCP 检验和也需要伪首部。

---

# TCP

![[Pasted image 20260617151546.png]]

## TCP 的特点

TCP 是在不可靠 IP 层之上实现的可靠传输协议。

特点：

1. 面向连接。
2. 每条 TCP 连接只能有两个端点。
3. 只支持一对一通信。
4. 提供可靠交付。
5. 全双工通信。
6. 面向字节流。
7. 有流量控制。
8. 有拥塞控制。

### TCP 面向字节流

TCP 把应用层交下来的数据看作连续字节流。

TCP 不保留应用层报文边界。

例如：

- 应用层交给 TCP 三次数据；
- TCP 可能合并成一次发送，也可能拆成多次发送。

对比：

| 协议 | 数据视角 |
|---|---|
| UDP | 面向报文 |
| TCP | 面向字节流 |

---

## TCP 报文段

TCP 报文段 = TCP 首部 + 数据。

TCP 首部：

- 最小长度：`20B`
- 最大长度：`60B`
- 首部长度必须是 `4B` 的整数倍

### TCP 首部字段

| 字段 | 长度 | 作用 |
|---|---:|---|
| 源端口 | 2B | 发送进程端口 |
| 目的端口 | 2B | 接收进程端口 |
| 序号 seq | 4B | 本报文段数据第一个字节的序号 |
| 确认号 ack | 4B | 期望收到的下一个字节序号 |
| 数据偏移 | 4 bit | TCP 首部长度，单位 4B |
| 控制位 | 若干 | 标识报文段性质 |
| 窗口 | 2B | 接收方当前允许发送方发送的数据量 |
| 检验和 | 2B | 检验伪首部、TCP 首部和数据 |
| 紧急指针 | 2B | URG=1 时有效 |
| 选项 | 可变 | 如 MSS、窗口扩大、时间戳 |
| 填充 | 可变 | 保证首部长度为 4B 整数倍 |

### 数据偏移

数据偏移字段表示 TCP 首部长度，单位是 `4B`。

`TCP 首部长度 = 数据偏移字段值 × 4B`

最小首部：

- `20B = 5 × 4B`
- 数据偏移最小值为 `5`

最大首部：

- `60B = 15 × 4B`
- 数据偏移最大值为 `15`

---

## TCP 序号与确认号

### 序号 seq

TCP 对字节编号，而不是对报文段编号。

若某报文段：

- `seq = 301`
- 数据长度 = `100B`

则它携带的数据字节序号为：

`301 ~ 400`

下一个报文段的序号应为：

`401`

### 确认号 ack

确认号表示：

`期望收到的下一个字节序号`

若 `ack = N`，表示：

`N - 1 及以前的所有字节都已正确收到`

例：

- 收到 `301 ~ 400`
- 确认号 `ack = 401`

TCP 默认采用累计确认。

---

## TCP 控制位

常见控制位：

| 控制位 | 含义 |
|---|---|
| ACK | 确认号有效 |
| SYN | 建立连接 |
| FIN | 释放连接 |
| RST | 复位连接 |
| PSH | 尽快交付应用层 |
| URG | 紧急指针有效 |

注意：

- 只有 ACK=1 时，确认号字段才有效。
- SYN=1 表示连接请求或连接接受。
- FIN=1 表示发送方数据发送完毕，请求释放连接。
- SYN 报文段不能携带数据，但消耗一个序号。
- FIN 报文段可以携带数据，也消耗一个序号。
- 普通 ACK 若不携带数据，不消耗序号。

---

# TCP 连接管理

TCP 连接经历三个阶段：

`连接建立 → 数据传输 → 连接释放`

---

## 三次握手

设客户端初始序号为 `x`，服务器初始序号为 `y`。

### 第一次握手

客户端发送：

- `SYN = 1`
- `seq = x`

客户端状态：`SYN-SENT`

注意：

- SYN 消耗一个序号。
- 下次客户端发送数据时序号为 `x + 1`。

### 第二次握手

服务器发送：

- `SYN = 1`
- `ACK = 1`
- `seq = y`
- `ack = x + 1`

服务器状态：`SYN-RCVD`

SYN 同样消耗一个序号。

### 第三次握手

客户端发送：

- `ACK = 1`
- `seq = x + 1`
- `ack = y + 1`

双方进入：`ESTABLISHED`

第三次握手可以携带数据。

若第三次握手不携带数据，则不消耗序号。

### 三次握手总结

| 次数 | 报文内容 |
|---|---|
| 第一次 | `SYN=1, seq=x` |
| 第二次 | `SYN=1, ACK=1, seq=y, ack=x+1` |
| 第三次 | `ACK=1, seq=x+1, ack=y+1` |

考点：

- 前两次握手 SYN=1。
- 第一次握手 ACK=0。
- 第二、三次握手 ACK=1。
- SYN 不能携带数据，但消耗序号。
- 第三次握手可以携带数据。
- 客户端最早 1 RTT 后可以发送数据。
- 服务器最早 1.5 RTT 后可以发送数据。

---

## 为什么需要三次握手

三次握手主要解决：

1. 双方确认彼此收发能力。
2. 协商初始序号。
3. 防止已失效的连接请求报文突然到达服务器，导致错误建立连接。

两次握手不够，因为服务器无法确认客户端是否收到了自己的确认。

---

## 四次挥手

设主动关闭方为客户端。

### 第一次挥手

客户端发送：

- `FIN = 1`
- `seq = u`

客户端进入：`FIN-WAIT-1`

FIN 消耗一个序号。

### 第二次挥手

服务器发送确认：

- `ACK = 1`
- `ack = u + 1`
- `seq = v`

服务器进入：`CLOSE-WAIT`

客户端收到后进入：`FIN-WAIT-2`

此时连接处于半关闭状态：

`客户端不能再发数据，服务器仍可继续发数据`

### 第三次挥手

服务器发送：

- `FIN = 1`
- `ACK = 1`
- `seq = w`
- `ack = u + 1`

服务器进入：`LAST-ACK`

### 第四次挥手

客户端发送最终确认：

- `ACK = 1`
- `seq = u + 1`
- `ack = w + 1`

客户端进入：`TIME-WAIT`

等待 `2MSL` 后进入：`CLOSED`

服务器收到最终 ACK 后进入：`CLOSED`

### 四次挥手总结

| 次数 | 报文内容 |
|---|---|
| 第一次 | `FIN=1, seq=u` |
| 第二次 | `ACK=1, seq=v, ack=u+1` |
| 第三次 | `FIN=1, ACK=1, seq=w, ack=u+1` |
| 第四次 | `ACK=1, seq=u+1, ack=w+1` |

考点：

- FIN 出现在第一次和第三次挥手。
- 后三次挥手 ACK=1。
- FIN 消耗序号。
- 普通 ACK 不携带数据时不消耗序号。
- TCP 连接释放通常需要四次挥手，因为 TCP 是全双工通信。

---

## TIME-WAIT 与 2MSL

主动关闭方在发送最后 ACK 后进入 TIME-WAIT，并等待 `2MSL`。

MSL：`Maximum Segment Lifetime，最长报文段寿命`

等待 2MSL 的作用：

1. 确保最后一个 ACK 能到达对方。
2. 让本连接中的旧报文段在网络中自然消失，避免影响新连接。

若服务器没有收到最后 ACK，会重传 FIN，客户端仍处于 TIME-WAIT，可以再次发送 ACK。

---

# TCP 可靠传输

TCP 通过以下机制实现可靠传输：

1. 检验和
2. 序号
3. 确认
4. 重传

---

## 确认机制

TCP 采用累计确认。

若接收方收到：

`1, 2, 3, 4, 5`

则发送：

`ack = 6`

表示 1~5 都已收到，期待 6。

若收到：

`1, 3, 4, 5`

缺少 2，则仍发送：

`ack = 2`

表示仍期待 2。

---

## 重传机制

TCP 触发重传的两类情况：

1. 超时
2. 收到 3 个冗余 ACK

### 超时重传

发送方每发送一个报文段，就为其设置一个超时计时器。

若超时仍未收到确认，则重传。

涉及概念：

| 名称 | 含义 |
|---|---|
| RTT | 往返时间 |
| RTTS | 加权平均往返时间 |
| RTO | 超时重传时间 |

RTO 应略大于 RTTS。

若 RTO 太小：

- 容易误重传。

若 RTO 太大：

- 丢包后恢复慢。

### 冗余 ACK 与快速重传

冗余 ACK：

`接收方再次确认已经确认过的报文段`

若发送方连续收到 `3 个相同的冗余 ACK`，认为后续某报文段丢失，立即重传。

注意：

`收到 3 个冗余 ACK = 总共收到 4 个相同 ACK`

快速重传不需要等待超时计时器到期。

---

# TCP 流量控制

## 基本思想

流量控制是为了防止发送方发送太快，导致接收方来不及接收。

本质：

`控制发送方发送速率，使其不超过接收方接收能力`

TCP 使用滑动窗口实现流量控制。

---

## 接收窗口 rwnd

接收方维护接收窗口：`rwnd`

表示接收方当前还能接收多少字节。

接收方通过 TCP 首部中的窗口字段把 rwnd 通知给发送方。

发送方的发送窗口不能超过接收方通告的 rwnd。

`发送窗口 ≤ rwnd`

---

## 零窗口问题

当接收方通告：

`rwnd = 0`

发送方停止发送数据。

问题：

`若接收方之后发送的非零窗口通知丢失，双方可能死锁`

解决：

`持续计时器`

发送方收到零窗口通知后启动持续计时器。

计时器超时后，发送方发送零窗口探测报文。

接收方返回当前窗口值：

- 若窗口仍为 0，发送方重启持续计时器。
- 若窗口非 0，发送方恢复发送。

---

## 流量控制与数据链路层滑动窗口的区别

| 比较 | TCP 流量控制 | 数据链路层流量控制 |
|---|---|---|
| 控制范围 | 端到端 | 相邻节点之间 |
| 控制对象 | 进程之间 | 链路两端 |
| 窗口大小 | 可动态变化 | 通常相对固定 |
| 依据 | 接收方缓存能力 | 链路层协议设计 |

---

# TCP 拥塞控制

## 拥塞控制目标

拥塞控制是为了防止过多数据注入网络，使路由器或链路过载。

本质：

`控制发送方发送速率，使其不超过网络承载能力`

### 拥塞控制与流量控制区别

| 比较 | 流量控制 | 拥塞控制 |
|---|---|---|
| 目的 | 保护接收方 | 保护网络 |
| 范围 | 端到端 | 全局性 |
| 依据 | 接收方缓存 | 网络拥塞程度 |
| 关键窗口 | rwnd | cwnd |

---

## 拥塞窗口 cwnd

发送方维护拥塞窗口：`cwnd`

发送窗口上限：

`发送窗口 = min(rwnd, cwnd)`

在拥塞控制题中，若题目假设接收窗口足够大，则：

`发送窗口 = cwnd`

通常以 MSS 为单位讨论 cwnd。

---

## 慢开始

慢开始思想：

`先小量发送，若未拥塞，再快速增大发送量`

初始：

`cwnd = 1 MSS`

每收到一个新的确认：

`cwnd = cwnd + 1`

每经过一个 RTT，cwnd 大约翻倍：

`1, 2, 4, 8, 16, ...`

注意：

慢开始的“慢”指从小窗口开始，不是增长速度慢。

---

## 拥塞避免

当：

`cwnd >= ssthresh`

进入拥塞避免阶段。

拥塞避免阶段：

`每经过一个 RTT，cwnd 加 1`

即线性增长：

`16, 17, 18, 19, ...`

### 慢开始与拥塞避免切换

| 条件 | 使用算法 |
|---|---|
| cwnd < ssthresh | 慢开始 |
| cwnd > ssthresh | 拥塞避免 |
| cwnd = ssthresh | 二者均可，通常按拥塞避免处理 |

---

## 出现超时后的处理

若发生超时，认为网络发生拥塞。

处理：

- `ssthresh = max(cwnd / 2, 2)`
- `cwnd = 1`
- 重新进入慢开始

常见题目中通常直接写：

- `ssthresh = cwnd / 2`
- `cwnd = 1`

但要注意 ssthresh 不小于 2 MSS。

---

## 快重传

接收方收到失序报文段时，应立即发送冗余 ACK。

发送方连续收到 3 个冗余 ACK 时，立即重传丢失报文段。

作用：

`不等超时，提前发现丢包`

---

## 快恢复

快恢复与快重传配合使用。

当发送方收到 3 个冗余 ACK：

- `ssthresh = cwnd / 2`
- `cwnd = ssthresh`
- 进入拥塞避免

区别于超时：

| 情况 | ssthresh | cwnd | 后续 |
|---|---|---|---|
| 超时 | cwnd/2 | 1 | 慢开始 |
| 3 个冗余 ACK | cwnd/2 | ssthresh | 快恢复，进入拥塞避免 |

---

# TCP 与 UDP 对比

| 比较 | TCP | UDP |
|---|---|---|
| 连接 | 面向连接 | 无连接 |
| 可靠性 | 可靠 | 不可靠 |
| 数据单位 | 字节流 | 报文 |
| 是否保留边界 | 不保留 | 保留 |
| 首部长度 | 最小 20B | 固定 8B |
| 通信方式 | 一对一 | 一对一、一对多、多对多 |
| 流量控制 | 有 | 无 |
| 拥塞控制 | 有 | 无 |
| 适用场景 | 文件传输、Web、邮件等 | DNS、DHCP、实时音视频等 |

---

# 高频计算与判断

## 1. UDP 长度

`UDP 长度 = UDP 首部 8B + UDP 数据长度`

若 UDP 数据长度为 100B：

`UDP 长度 = 8 + 100 = 108B`

---

## 2. TCP 数据偏移

`TCP 首部长度 = 数据偏移 × 4B`

若数据偏移为 6：

`TCP 首部长度 = 6 × 4B = 24B`

选项长度：

`选项长度 = TCP 首部长度 - 20B`

---

## 3. TCP 序号

若：

- `seq = x`
- 数据长度 = `L`

则本报文段数据序号范围：

`x ~ x + L - 1`

下一个报文段序号：

`x + L`

---

## 4. TCP 确认号

若接收方已经连续收到到第 `k` 字节：

`ack = k + 1`

若中间缺失，则确认号停在缺失的第一个字节。

---

## 5. SYN / FIN / ACK 是否消耗序号

| 报文类型 | 是否消耗序号 |
|---|---|
| SYN | 消耗 1 个序号 |
| FIN | 消耗 1 个序号 |
| 纯 ACK | 不消耗序号 |
| 携带数据的 ACK | 消耗数据长度对应的序号 |

---

## 6. 发送窗口

一般：

`发送窗口 = min(rwnd, cwnd)`

若题目说明接收窗口足够大：

`发送窗口 = cwnd`

---

## 7. 慢开始轮次

若初始：

`cwnd = 1`

每个 RTT 后：

`1 → 2 → 4 → 8 → 16`

这是指数增长。

---

## 8. 拥塞避免轮次

进入拥塞避免后，每个 RTT：

`cwnd = cwnd + 1`

这是线性增长。

---

# 易错点总结

1. 传输层提供的是进程到进程通信，不是主机到主机通信。
2. 端口号只有本地意义。
3. TCP 和 UDP 的端口号空间独立。
4. UDP 首部固定 8B。
5. UDP 面向报文，TCP 面向字节流。
6. UDP 检验和使用伪首部，但伪首部不真正传输。
7. TCP 首部最小 20B，最大 60B。
8. TCP 数据偏移字段单位是 4B。
9. TCP 序号按字节编号，不按报文段编号。
10. 确认号表示期望收到的下一个字节序号。
11. SYN 消耗序号，FIN 消耗序号，纯 ACK 不消耗序号。
12. 第三次握手可以携带数据。
13. 四次挥手是因为 TCP 是全双工通信。
14. TIME-WAIT 等待 2MSL 是为了可靠释放连接和清除旧报文。
15. 流量控制看接收方能力，拥塞控制看网络承载能力。
16. 发送窗口取 `rwnd` 和 `cwnd` 的较小值。
17. 超时后 `cwnd = 1`，重新慢开始。
18. 收到 3 个冗余 ACK 后快重传，并进入快恢复。
19. 快恢复时 `cwnd` 不变成 1，而是变为 `ssthresh`。
20. 拥塞避免不能完全避免拥塞，只是让窗口线性增长，降低拥塞风险。